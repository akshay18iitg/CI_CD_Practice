name: 01- Building Blocks
on:
 - workflow_dispatch

jobs:
  first-job:
   runs-on: ubuntu-latest
   steps:
    - run: echo "Hello world"
  
  second-job:
   runs-on: ubuntu-latest
   steps:
    - name: Failed step
      run: |
        echo "I will fail"
        exit 0
    - name: Say goodbye
      run: echo "Goodbye!" 

name: 02- Workflow Events
on:
  - push
  - schedule
  - workflow_dispatch

jobs:
  Echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "${{github.event_name }}" 